clinksplatform:
  customerlinks:
    connection:
      base-url:
      auth-url:
      username:
      password:
      clientId:
      clientSecret:
      topic:
      batch-topic:
      replayform:
      max-buffer-size:
      max-network-delay:
      grantType:
      use-proxy:
      use-proxy-for-auth:
      use-ssl:
    api:
      version:
      endpoint:

  zbus:
    connection:
      auth-url:
      base-url:
      grant_type:
      client-name:
      stream-id:
      client-id:
      client-secret:
      scope:
      limit:
      use-proxy:
      use-proxy-for-auth:
      use-ssl:

  rice:
    connection:
      truststore-password:
      certificate-alias:
      zbus:
        connection:
          auth-url:
          base-url:
          grant_type:
          client-name:
          client-id:
          client-secret:
          scope:
          limit:
          use-proxy:
          use-proxy-for-auth:
          use-ssl:
  security:
    truststore-password:
    certificate-alias:
  resiliency:
    timeout-in-seconds:
    retry-number:
logging:
  level:
    reactor:
      netty:
        tcp:
          TcpClient:
    com.MyProject:
      mediationplatform:
        common.network.WebClientConfigurer:
        common.network.CacheProvider:
      mediationplatformcustomerconsumer.service.network.ClientAuthenticator:
